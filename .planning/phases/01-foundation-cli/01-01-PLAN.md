---
phase: 01-foundation-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/db-sync/main.go
  - internal/cli/app.go
  - internal/cli/root.go
  - internal/cli/profile_command.go
  - internal/model/profile.go
  - internal/profile/schema.go
  - internal/profile/store.go
  - internal/secrets/template.go
  - internal/profile/profile_test.go
  - internal/secrets/template_test.go
autonomous: true
requirements:
  - PROF-01
must_haves:
  truths:
    - Operator can start a Go CLI binary and reach a wizard-first profile workflow entrypoint.
    - The saved-profile schema and placeholder-safe serialization contract are defined and tested for later persistence work.
    - Secret values are represented as environment placeholders rather than stored credentials.
    - The program can read an .env file given as parameter
  artifacts:
    - path: go.mod
      provides: Module bootstrap and phase test dependencies.
    - path: cmd/db-sync/main.go
      provides: CLI entrypoint that boots the Cobra command tree.
    - path: internal/model/profile.go
      provides: Stable profile domain structs and defaults.
    - path: internal/profile/store.go
      provides: Store and validator interfaces for later plans.
    - path: internal/secrets/template.go
      provides: Placeholder parsing and runtime resolution contract.
  key_links:
    - from: cmd/db-sync/main.go
      to: internal/cli/root.go
      via: NewRootCommand bootstrap
      pattern: NewRootCommand
    - from: internal/cli/profile_command.go
      to: internal/profile/store.go
      via: interface-backed app dependencies
      pattern: ProfileStore|ProfileValidator
    - from: internal/model/profile.go
      to: internal/secrets/template.go
      via: dsn_template fields use env placeholders
      pattern: dsn_template|ResolveTemplate
---

<objective>
Define the Phase 1 foundation contracts: Go module bootstrap, wizard-first command topology, profile schema, and placeholder-safe persistence interfaces.

Purpose: Create the exact contracts that Plans 01-02 and 01-03 can build against without rediscovering structure.
Output: A bootable CLI shell, explicit profile model, placeholder resolver contract, and unit tests covering schema round-trip behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-RESEARCH.md
@.planning/phases/01-foundation-cli/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Go module and wizard-first Cobra command shell</name>
  <files>go.mod, go.sum, cmd/db-sync/main.go, internal/cli/app.go, internal/cli/root.go, internal/cli/profile_command.go</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-cli/01-CONTEXT.md, .planning/phases/01-foundation-cli/01-RESEARCH.md, .planning/phases/01-foundation-cli/01-VALIDATION.md</read_first>
  <action>Initialize the module as `db-sync`; add Cobra, Huh, Lip Gloss, xdg, yaml.v3, go-cmp, and testcontainers-go dependencies so `go test ./...` is viable from Phase 1 onward; create `cmd/db-sync/main.go` that boots `internal/cli.NewRootCommand`; add `internal/cli/root.go` so `db-sync` with no args launches the interactive profile flow only when stdin is a terminal per D-01 and D-02, otherwise returns help; add `internal/cli/profile_command.go` with explicit `profile new`, `profile edit &lt;name&gt;`, `profile list`, and `profile validate &lt;name&gt;` subcommands; define `internal/cli/app.go` with interface-backed dependencies and handler method signatures so later plans can plug in wizard and persistence logic without changing the command layout.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go mod edit -json | rg '"Module": "db-sync"'`
    - `rg 'Use:\s+"db-sync"' internal/cli/root.go`
    - `rg 'profile new|profile edit <name>|profile list|profile validate <name>' internal/cli/profile_command.go`
    - `rg 'IsTerminal|term\.IsTerminal|terminal' internal/cli/root.go`
  </acceptance_criteria>
  <done>The repository compiles as a Go module, the root command is bootstrapped, and the command tree exposes the Phase 1 profile entrypoints without implementing persistence yet.</done>
</task>

<task type="auto">
  <name>Task 2: Define the forward-compatible profile model and placeholder-safe service contracts</name>
  <files>internal/model/profile.go, internal/profile/schema.go, internal/profile/store.go, internal/secrets/template.go</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-foundation-cli/01-CONTEXT.md, .planning/phases/01-foundation-cli/01-RESEARCH.md, internal/cli/app.go, internal/cli/profile_command.go</read_first>
  <action>Create `internal/model/profile.go` with explicit structs for `Profile`, `Endpoint`, `Selection`, and `SyncOptions`; include `version`, `name`, `source.engine`, `source.dsn_template`, `target.engine`, `target.dsn_template`, `selection.tables`, `sync.mode`, and `sync.mirror_delete`; set conservative defaults matching research: empty `selection.tables`, `sync.mode = insert-missing`, `sync.mirror_delete = false`; add `internal/profile/schema.go` for YAML marshal/unmarshal helpers and slug rules for one-file-per-profile naming; add `internal/profile/store.go` with `ProfileStore` and `ProfileValidator` interfaces, plus structured validation report types; add `internal/secrets/template.go` that only supports `${NAME}` placeholders per D-05, rejects invalid placeholder names, and never exposes resolved values in string output.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type Profile struct' internal/model/profile.go`
    - `rg 'Selection struct|tables \[\]string' internal/model/profile.go`
    - `rg 'MirrorDelete bool|mirror_delete' internal/model/profile.go`
    - `rg 'type ProfileStore interface|type ProfileValidator interface' internal/profile/store.go`
    - `rg '\$\\\{\([A-Z]\[A-Z0-9_\]\*\)\\\}' internal/secrets/template.go`
  </acceptance_criteria>
  <done>The domain model is explicit, profile files are structured for future phases, and command handlers can depend on interfaces rather than concrete persistence or validation code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the schema and placeholder behavior with unit tests</name>
  <files>internal/profile/profile_test.go, internal/secrets/template_test.go</files>
  <read_first>.planning/phases/01-foundation-cli/01-VALIDATION.md, internal/model/profile.go, internal/profile/schema.go, internal/profile/store.go, internal/secrets/template.go</read_first>
  <behavior>
    - Test 1: a profile round-trip through YAML preserves `version`, endpoint engines, `dsn_template`, empty `selection.tables`, and default sync settings.
    - Test 2: placeholder parsing returns referenced environment variable names without resolving them into persisted output.
    - Test 3: invalid placeholders such as `${db_password}` or `${BAD-NAME}` return an error.
  </behavior>
  <action>Write unit tests before implementation details are finalized; assert that serialized YAML never contains resolved secret values and that placeholder discovery is deterministic; keep tests package-local to the files above and avoid introducing integration containers in this plan.</action>
  <verify>
    <automated>go test ./internal/profile/... ./internal/secrets/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'TestProfile.*RoundTrip|Test.*YAML' internal/profile/profile_test.go`
    - `rg 'Test.*Placeholder' internal/secrets/template_test.go`
    - `go test ./internal/profile/... ./internal/secrets/...`
  </acceptance_criteria>
  <done>Phase 1 has executable guardrails for the profile schema and placeholder rules, and later plans can rely on those contracts instead of rediscovering them.</done>
</task>

</tasks>

<verification>
Run `go test ./...` and confirm the root command tree, profile model, and placeholder tests all compile and pass from a clean checkout.</verification>

<success_criteria>
The repository contains a bootable CLI shell, explicit profile contracts, and automated tests proving the saved-profile schema is placeholder-safe and future-phase ready.</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-01-SUMMARY.md`.
</output>