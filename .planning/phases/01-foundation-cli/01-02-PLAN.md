---
phase: 01-foundation-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/wizard/draft.go
  - internal/wizard/flow.go
  - internal/wizard/review.go
  - internal/wizard/messages.go
  - internal/wizard/wizard_test.go
  - internal/cli/profile_new.go
  - internal/cli/profile_edit.go
autonomous: true
requirements:
  - PROF-01
  - PROF-02
must_haves:
  truths:
    - Operator can start a fast-path interactive setup flow instead of assembling long flag strings.
    - Operator can reopen an existing profile in the same flow with current values prefilled.
    - Operator sees a review-before-save step that summarizes placeholder-based connection data and default Phase 1 sync state.
  artifacts:
    - path: internal/wizard/draft.go
      provides: Draft model shared by new and edit flows.
    - path: internal/wizard/flow.go
      provides: Huh-based flow orchestration for create and edit.
    - path: internal/wizard/review.go
      provides: Review-before-save renderer.
    - path: internal/cli/profile_new.go
      provides: Command handler for new profile flow.
    - path: internal/cli/profile_edit.go
      provides: Command handler for edit profile flow.
  key_links:
    - from: internal/cli/profile_new.go
      to: internal/wizard/flow.go
      via: StartNew or equivalent flow entrypoint
      pattern: StartNew|RunNewFlow
    - from: internal/cli/profile_edit.go
      to: internal/wizard/draft.go
      via: draft hydration from existing profile
      pattern: FromProfile|HydrateDraft
    - from: internal/wizard/review.go
      to: internal/model/profile.go
      via: review rendering maps draft to profile fields
      pattern: dsn_template|mirror_delete|tables
---

<objective>
Implement the interactive profile creation and editing experience with a fast-path wizard, shared draft model, and review-before-save step.

Purpose: Deliver the operator-facing workflow for PROF-02 while keeping the interaction model aligned with the locked Phase 1 decisions.
Output: Reusable wizard flow code, new/edit command handlers, and tests proving edit mode reuses the same flow with prefilled values.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-RESEARCH.md
@.planning/phases/01-foundation-cli/01-VALIDATION.md
@cmd/db-sync/main.go
@internal/cli/app.go
@internal/cli/root.go
@internal/cli/profile_command.go
@internal/model/profile.go
@internal/profile/store.go
@internal/secrets/template.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the reusable Huh draft flow for new and edit profile sessions</name>
  <files>internal/wizard/draft.go, internal/wizard/flow.go, internal/wizard/messages.go</files>
  <read_first>.planning/phases/01-foundation-cli/01-CONTEXT.md, .planning/phases/01-foundation-cli/01-RESEARCH.md, internal/model/profile.go, internal/profile/store.go, internal/secrets/template.go, internal/cli/app.go</read_first>
  <action>Create `internal/wizard/draft.go` with a `ProfileDraft` type and helpers that convert between `model.Profile` and the form state; implement `internal/wizard/flow.go` using Huh groups so the setup experience explicitly satisfies D-06 by optimizing for a fast-path-first sequence instead of a long explicit wizard: profile name, source engine, source DSN template, target engine, target DSN template, sync mode, and mirror-delete confirmation, without introducing table selection in Phase 1 per D-11; edit mode must hydrate the same draft from an existing profile per D-07; add `internal/wizard/messages.go` for explicit operator-facing copy around placeholder requirements, future table selection, and save gating.</action>
  <verify>
    <automated>go test ./internal/wizard/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type ProfileDraft struct' internal/wizard/draft.go`
    - `rg 'FromProfile|HydrateDraft' internal/wizard/draft.go`
    - `rg 'huh\.NewForm|huh\.NewGroup' internal/wizard/flow.go`
    - `rg 'mirror delete|environment variable|table selection happens in a later phase' internal/wizard/messages.go -i`
  </acceptance_criteria>
  <done>The wizard flow is defined once, handles both new and edit entrypoints, and encodes the Phase 1 fast-path behavior without table-selection logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add review-before-save rendering and command handlers for new and edit</name>
  <files>internal/wizard/review.go, internal/cli/profile_new.go, internal/cli/profile_edit.go</files>
  <read_first>.planning/phases/01-foundation-cli/01-CONTEXT.md, internal/cli/root.go, internal/cli/profile_command.go, internal/wizard/draft.go, internal/wizard/flow.go, internal/model/profile.go</read_first>
  <action>Create `internal/wizard/review.go` that renders a concise pre-save summary with profile name, engines, DSN templates showing placeholder tokens only, `selection.tables` as empty in Phase 1, and sync settings; wire `internal/cli/profile_new.go` so the root interactive path and `profile new` both invoke the wizard and stop at a reviewed `model.Profile` draft; wire `internal/cli/profile_edit.go` so `profile edit &lt;name&gt;` loads an existing profile through the store interface, reopens the same wizard with prefilled values, and returns to the same review step per D-08; do not persist profiles in this plan, only route through the app interfaces introduced in 01-01.</action>
  <verify>
    <automated>go test ./internal/cli/... ./internal/wizard/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'Review|review-before-save|ConfirmSave' internal/wizard/review.go -i`
    - `rg 'StartNew|RunNewFlow' internal/cli/profile_new.go`
    - `rg 'Load\(|profile edit' internal/cli/profile_edit.go`
    - `rg 'dsn_template|tables|mirror_delete' internal/wizard/review.go`
  </acceptance_criteria>
  <done>Operators can launch new or edit flows that share one wizard implementation and always end with an explicit review step before any save attempt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove edit reuse and review behavior with wizard-focused tests</name>
  <files>internal/wizard/wizard_test.go</files>
  <read_first>.planning/phases/01-foundation-cli/01-VALIDATION.md, internal/wizard/draft.go, internal/wizard/flow.go, internal/wizard/review.go, internal/cli/profile_new.go, internal/cli/profile_edit.go</read_first>
  <behavior>
    - Test 1: creating a draft from scratch applies Phase 1 defaults for empty `selection.tables`, `sync.mode`, and `sync.mirror_delete`.
    - Test 2: hydrating from an existing profile pre-fills the same draft fields used by the new flow.
    - Test 3: the review renderer includes placeholder names and sync settings but excludes resolved secret values.
  </behavior>
  <action>Add unit tests in `internal/wizard/wizard_test.go` that exercise new-draft defaults, edit-draft hydration, and review output content; if helper seams are needed, extract small pure functions from the wizard package rather than adding UI-only mocks.</action>
  <verify>
    <automated>go test ./internal/wizard/... ./internal/cli/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'Test.*Draft.*Default|Test.*Edit.*Prefill|Test.*Review' internal/wizard/wizard_test.go`
    - `go test ./internal/wizard/... ./internal/cli/...`
  </acceptance_criteria>
  <done>The new/edit flow is covered by automated tests that lock in fast-path defaults, edit prefill behavior, and review-step secret hygiene.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/wizard/... ./internal/cli/...` and confirm the wizard packages compile cleanly against the contracts created in Plan 01.</verification>

<success_criteria>
The CLI has a reusable create/edit wizard with a review step, and automated tests prove edit mode is the same flow with existing values prefilled.</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-02-SUMMARY.md`.
</output>