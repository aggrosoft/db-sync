---
phase: 01-foundation-cli
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - internal/cli/app.go
  - internal/cli/profile_command.go
  - internal/cli/profile_edit.go
  - internal/profile/filesystem_store.go
  - internal/profile/filesystem_store_test.go
  - internal/validate/service.go
  - internal/validate/service_test.go
  - internal/validate/validate_integration_test.go
  - internal/db/postgres/adapter.go
  - internal/db/mysql/adapter.go
  - internal/testkit/containers.go
  - internal/cli/profile_validate.go
  - internal/cli/profile_save.go
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
must_haves:
  truths:
    - A profile is only written to disk after both source and target validations succeed.
    - Operators can validate saved profiles on demand and understand why validation failed without seeing secrets.
    - Saved profiles live in the per-user config directory as YAML files and can be listed or reloaded by name.
  artifacts:
    - path: internal/profile/filesystem_store.go
      provides: XDG-backed YAML save/load/list implementation.
    - path: internal/validate/service.go
      provides: Orchestration for dual-endpoint validation and save gating.
    - path: internal/db/postgres/adapter.go
      provides: PostgreSQL authentication, metadata, and capability probes.
    - path: internal/db/mysql/adapter.go
      provides: MySQL or MariaDB authentication, metadata, and capability probes.
    - path: internal/cli/profile_save.go
      provides: Wiring from reviewed wizard output to gated persistence.
    - path: internal/cli/profile_validate.go
      provides: On-demand validation command for saved profiles with redacted operator output.
  key_links:
    - from: internal/cli/profile_save.go
      to: internal/validate/service.go
      via: reviewed profile passes through validation before store save
      pattern: ValidateAndSave|ValidateProfile
    - from: internal/cli/profile_validate.go
      to: internal/validate/service.go
      via: saved-profile validation delegates to the shared validation service
      pattern: ValidateProfile
    - from: internal/validate/service.go
      to: internal/db/postgres/adapter.go
      via: source and target adapter dispatch
      pattern: ValidateSource|ValidateTarget
    - from: internal/profile/filesystem_store.go
      to: internal/model/profile.go
      via: YAML round-trip in per-user config directory
      pattern: yaml|xdg
---

<objective>
Implement connection validation, gated profile persistence, and load/list/validate behavior for saved profiles.

Purpose: Complete the operator-visible Phase 1 workflow so reviewed profiles are only persisted after safe source and target validation succeeds.
Output: Concrete profile store, validation service with engine adapters, CLI save/load hooks, and automated tests for save blocking and connectivity checks.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-RESEARCH.md
@.planning/phases/01-foundation-cli/01-VALIDATION.md
@internal/cli/app.go
@internal/cli/profile_command.go
@internal/cli/profile_new.go
@internal/cli/profile_edit.go
@internal/model/profile.go
@internal/profile/schema.go
@internal/profile/store.go
@internal/secrets/template.go
@internal/wizard/draft.go
@internal/wizard/review.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement XDG-backed profile storage and validation orchestration</name>
  <files>internal/cli/app.go, internal/profile/filesystem_store.go, internal/profile/filesystem_store_test.go, internal/validate/service.go, internal/validate/service_test.go</files>
  <read_first>.planning/phases/01-foundation-cli/01-CONTEXT.md, .planning/phases/01-foundation-cli/01-RESEARCH.md, internal/model/profile.go, internal/profile/schema.go, internal/profile/store.go, internal/secrets/template.go, internal/cli/app.go</read_first>
  <action>Create `internal/profile/filesystem_store.go` using `github.com/adrg/xdg` so profiles are persisted as one YAML file per slugged name in the user config directory per D-03; implement `Save`, `Load`, `List`, and path resolution without storing resolved DSNs or raw secrets per D-04 and D-05; create `internal/validate/service.go` with a `ValidateProfile` and `ValidateAndSave` orchestration layer that resolves placeholders from the process environment, returns structured reports, and blocks persistence until both source and target succeed per D-09; update `internal/cli/app.go` so the composition root can construct and expose the concrete filesystem store plus validation service dependencies needed by `profile new`, `profile edit`, `profile list`, and `profile validate`; add unit tests for slug/path resolution, duplicate names, missing placeholders, and blocked save behavior.</action>
  <verify>
    <automated>go test ./internal/profile/... ./internal/validate/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'xdg' internal/profile/filesystem_store.go`
    - `rg 'Save\(|Load\(|List\(' internal/profile/filesystem_store.go`
    - `rg 'ValidateAndSave|ValidateProfile' internal/validate/service.go`
    - `rg 'missing required environment variables|blocked save' internal/validate/service_test.go -i`
  </acceptance_criteria>
  <done>The application can resolve where profiles belong, load them safely, and block saves whenever placeholder resolution or connection validation fails.</done>
</task>

<task type="auto">
  <name>Task 2: Add PostgreSQL and MySQL or MariaDB validation adapters plus CLI save and validate hooks</name>
  <files>internal/db/postgres/adapter.go, internal/db/mysql/adapter.go, internal/cli/profile_command.go, internal/cli/profile_edit.go, internal/cli/profile_validate.go, internal/cli/profile_save.go</files>
  <read_first>.planning/phases/01-foundation-cli/01-CONTEXT.md, .planning/phases/01-foundation-cli/01-RESEARCH.md, internal/validate/service.go, internal/profile/filesystem_store.go, internal/cli/app.go, internal/cli/profile_command.go, internal/cli/profile_new.go, internal/cli/profile_edit.go, internal/wizard/review.go</read_first>
  <action>Implement `internal/db/postgres/adapter.go` using pgx and `internal/db/mysql/adapter.go` using go-sql-driver/mysql; each adapter must perform the Phase 1 probe sequence from D-10: authenticate, confirm metadata access through `information_schema`, and run a lightweight non-mutating target capability check that detects obvious read-only or standby states; create `internal/cli/profile_save.go` so the reviewed output of the new/edit wizard flows through `ValidateAndSave`; create `internal/cli/profile_validate.go` for `profile validate &lt;name&gt;` against a saved profile and keep all output redacted so placeholder names may appear but resolved secrets never do; update `internal/cli/profile_command.go` so `profile list` delegates to the concrete store `List()` implementation and update `internal/cli/profile_edit.go` so edit mode reloads the named profile through the store-backed app dependency before reopening the wizard.</action>
  <verify>
    <automated>go test ./internal/db/... ./internal/cli/... ./internal/validate/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'information_schema' internal/db/postgres/adapter.go internal/db/mysql/adapter.go`
    - `rg 'ValidateTarget|ValidateSource' internal/db/postgres/adapter.go internal/db/mysql/adapter.go`
    - `rg 'ValidateAndSave' internal/cli/profile_save.go`
    - `rg 'profile validate' internal/cli/profile_validate.go -i`
    - `rg 'List\(|profile list' internal/cli/profile_command.go internal/cli/profile_edit.go`
  </acceptance_criteria>
  <done>Source and target endpoints can be validated through engine-specific adapters, and the wizard flow is now connected to gated persistence instead of stopping at review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add DB-backed integration coverage for dual-endpoint validation</name>
  <files>internal/validate/validate_integration_test.go, internal/testkit/containers.go</files>
  <read_first>.planning/phases/01-foundation-cli/01-VALIDATION.md, internal/validate/service.go, internal/db/postgres/adapter.go, internal/db/mysql/adapter.go, internal/profile/filesystem_store.go</read_first>
  <behavior>
    - Test 1: PostgreSQL source and target endpoints validate successfully when credentials and metadata access are correct.
    - Test 2: MySQL or MariaDB source and target endpoints validate successfully with the same probe contract.
    - Test 3: validation failure for one endpoint prevents `ValidateAndSave` from writing the profile file.
  </behavior>
  <action>Create `internal/testkit/containers.go` with reusable testcontainers helpers for PostgreSQL and MySQL or MariaDB; add `internal/validate/validate_integration_test.go` behind the `integration` build tag and assert the orchestration layer produces success reports for healthy containers and no persisted file for failing cases; keep runtime under the ~45 second validation target from `01-VALIDATION.md`.</action>
  <verify>
    <automated>go test ./internal/validate/... -tags=integration</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'go:build integration|\+build integration' internal/validate/validate_integration_test.go`
    - `rg 'testcontainers' internal/testkit/containers.go internal/validate/validate_integration_test.go -i`
    - `go test ./... -tags=integration`
  </acceptance_criteria>
  <done>Phase 1 has automated proof that both supported engine families pass the same validation contract and that failed validation blocks profile persistence.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/profile/... ./internal/validate/... ./internal/db/... ./internal/cli/...` and then `go test ./... -tags=integration` to confirm save gating and adapter probes behave correctly.</verification>

<success_criteria>
Operators can create or edit a profile, validate both endpoints before save, persist the profile in the per-user config directory only on success, and re-run validation against a saved profile.</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-03-SUMMARY.md`.
</output>