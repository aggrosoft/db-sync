---
phase: 01-foundation-cli
plan: 06
type: execute
wave: 5
depends_on:
  - 01-03
  - 01-05
files_modified:
  - internal/profile/store.go
  - internal/profile/filesystem_store.go
  - internal/profile/filesystem_store_test.go
  - internal/validate/service.go
  - internal/validate/service_test.go
  - internal/validate/validate_integration_test.go
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
gap_closure: true
must_haves:
  truths:
    - Saving a connection-first profile writes an env-backed representation automatically, keeping secrets out of YAML while preserving the existing review-before-save and blocked-save guarantees.
    - Validation resolves the saved connection model from the profile env file plus process or CLI overrides, so operators do not have to author DSN templates manually.
    - Existing Phase 1 profiles that only contain `dsn_template` continue to load, edit, and validate so the gap closure does not strand earlier saved configs.
  artifacts:
    - path: internal/profile/filesystem_store.go
      provides: YAML plus sibling env-file persistence for the connection-first profile model.
    - path: internal/profile/filesystem_store_test.go
      provides: Automated save/load coverage for env-backed persistence and legacy profile compatibility.
    - path: internal/validate/service.go
      provides: Validation that can build resolved DSNs from the new connection representation.
    - path: internal/validate/validate_integration_test.go
      provides: Real PostgreSQL and MySQL validation coverage for generated connection input.
  key_links:
    - from: internal/profile/filesystem_store.go
      to: internal/profile/store.go
      via: save and load paths persist the new endpoint schema plus env-file convention
      pattern: Save\(|Load\(|env
    - from: internal/validate/service.go
      to: internal/profile/filesystem_store.go
      via: validation resolves saved connection data and env-backed secrets before connect
      pattern: ValidateProfile|ValidateAndSave|env
    - from: internal/validate/service.go
      to: internal/secrets/template.go
      via: generated DSN templates and env resolution stay redacted and placeholder-safe
      pattern: ResolveTemplate|ValidateTemplatePolicy
---

<objective>
Finish the connection-first UAT gap by persisting the env-backed profile representation, validating it end-to-end, and preserving legacy `dsn_template` compatibility.

Purpose: Operator-friendly input only solves the problem if save, reload, edit, and validation all work without manual template authoring and without stranding existing Phase 1 profiles.
Output: Env-backed profile persistence, validation that resolves the new connection model, and backward-compatible save/load behavior covered by unit and integration tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-UAT.md
@.planning/phases/01-foundation-cli/01-03-SUMMARY.md
@.planning/phases/01-foundation-cli/01-05-SUMMARY.md
@internal/profile/store.go
@internal/profile/filesystem_store.go
@internal/validate/service.go

<interfaces>
From internal/profile/store.go:
```go
type Store interface {
    Save(ctx context.Context, candidate model.Profile) error
    Load(ctx context.Context, name string) (model.Profile, error)
}
```

From internal/validate/service.go:
```go
func (service *Service) ValidateProfile(ctx context.Context, candidate model.Profile) (profile.ValidationReport, error)
func (service *Service) ValidateAndSave(ctx context.Context, candidate model.Profile) (profile.ValidationReport, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist the connection-first profile as YAML plus sibling env file with legacy load compatibility</name>
  <files>internal/profile/store.go, internal/profile/filesystem_store.go, internal/profile/filesystem_store_test.go</files>
  <behavior>
    - Test 1: saving a new connection-first profile writes YAML plus a sibling `.env` file under the profile config directory, with YAML containing only env references or generated templates and never raw secrets.
    - Test 2: loading a saved connection-first profile reconstructs the mode and non-secret values needed for later edit prefill.
    - Test 3: an older Phase 1 YAML file containing only `engine` plus `dsn_template` still loads successfully through the compatibility path.
  </behavior>
  <action>Update `internal/profile/store.go` and `internal/profile/filesystem_store.go` so saving a profile also writes or updates a sibling env file such as `<slug>.env` in the same config directory; persist the env-file location in the schema or derive it by convention, but keep YAML limited to env references and non-secret connection metadata. When loading, support both the new connection-first schema from 01-05 and the legacy `dsn_template`-only shape, normalizing the latter into the `legacy-template` mode so edit and validation can keep working. Refresh `internal/profile/filesystem_store_test.go` to cover env-backed save/load behavior and the backward-compatible legacy path. Do not move validation into this task.</action>
  <verify>
    <automated>go test ./internal/profile/...</automated>
  </verify>
  <done>Profiles persist as YAML plus env file without leaking secrets, saved profiles reload into the new connection model, and older template-only profiles still load.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve and validate connection-first profiles end-to-end without manual DSN template authoring</name>
  <files>internal/validate/service.go, internal/validate/service_test.go, internal/validate/validate_integration_test.go</files>
  <behavior>
    - Test 1: validating a saved connection-first profile builds the final DSN from the stored connection model and merged env sources rather than requiring a handwritten template.
    - Test 2: CLI overrides such as `--env-file` still win over the profile env file where applicable.
    - Test 3: failed validation still prevents YAML persistence, while legacy template-only profiles remain valid.
  </behavior>
  <action>Update `internal/validate/service.go` so validation can build or resolve the final DSN from either connection mode, merging the process environment with the profile-specific env file while preserving CLI overrides from `--env-file` and the blocked-save contract from 01-03. Keep compatibility with `legacy-template` profiles so previously saved configs remain editable and valid. Refresh `internal/validate/service_test.go` and `internal/validate/validate_integration_test.go` to cover connection-string mode, details mode, env-file override precedence, validation failure behavior, and real PostgreSQL plus MySQL validation using the generated connection representation.</action>
  <verify>
    <automated>go test ./internal/validate/...; go test ./internal/validate/... -tags=integration</automated>
  </verify>
  <done>Validation works from operator-friendly connection input, still blocks failed saves, honors env override precedence, and preserves legacy profile behavior.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/profile/... ./internal/validate/...` and then `go test ./internal/validate/... -tags=integration` to prove save, load, edit, and validation all work from the env-backed connection representation while legacy template profiles remain valid.</verification>

<success_criteria>
Phase 1 accepts operator-friendly connection input end-to-end: it persists secrets outside YAML, validates against the generated connection representation, and keeps earlier template-only profiles functional.</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-06-SUMMARY.md`.
</output>