---
phase: 01-foundation-cli
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - internal/cli/root.go
  - internal/cli/root_test.go
autonomous: true
requirements:
  - PROF-01
gap_closure: true
must_haves:
  truths:
    - Running `go run ./cmd/db-sync` with no args in normal Windows operator terminals enters the profile wizard instead of printing help.
    - Truly non-interactive execution, such as piped stdin or redirected input, still falls back to help rather than hanging in the wizard.
    - The root-command startup decision is covered by automated regression tests instead of relying on ad hoc terminal behavior.
  artifacts:
    - path: internal/cli/root.go
      provides: Root-command startup policy for no-arg execution.
    - path: internal/cli/root_test.go
      provides: Unit coverage for interactive and non-interactive startup branches.
  key_links:
    - from: internal/cli/root.go
      to: internal/cli/app.go
      via: StartInteractiveProfile on no-arg interactive entry
      pattern: StartInteractiveProfile
    - from: internal/cli/root.go
      to: cobra help rendering
      via: no-arg non-interactive fallback
      pattern: Help\(
---

<objective>
Close the diagnosed root-command startup gap by replacing the brittle terminal gate with a Windows-tolerant interactivity check and regression tests.

Purpose: Phase 1 promises a wizard-first default entrypoint, and the current `term.IsTerminal(0)` check breaks that promise in expected Windows integrated or pseudo-terminal contexts.
Output: A hardened no-arg root-command gate plus tests that prove the wizard starts in expected operator shells without regressing piped or redirected usage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-UAT.md
@.planning/phases/01-foundation-cli/01-02-SUMMARY.md
@internal/cli/root.go
@internal/cli/app.go

<interfaces>
From internal/cli/root.go:
```go
func NewRootCommand(app *App) *cobra.Command
```

From internal/cli/app.go:
```go
func (app *App) StartInteractiveProfile(ctx context.Context) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the brittle terminal probe with a layered Windows-aware interactivity gate</name>
  <files>internal/cli/root.go, internal/cli/root_test.go</files>
  <behavior>
    - Test 1: no args plus a normal Windows terminal context starts the wizard even when `golang.org/x/term` alone would return false.
    - Test 2: piped or redirected stdin still returns help instead of starting the wizard.
    - Test 3: explicit subcommands such as `profile list` are unaffected by the new root-only interactivity logic.
  </behavior>
  <action>Refactor `internal/cli/root.go` so the root `RunE` no longer relies on `term.IsTerminal(0)` alone. Add an internal helper or injectable probe that treats the session as interactive when stdin is not redirected and at least one expected terminal signal is present for the tested Windows shells, such as `term.IsTerminal(fd)`, a character-device stdin handle, `WT_SESSION`, `TERM_PROGRAM=vscode`, `ConEmuPID`, `ANSICON`, or a non-empty `TERM`. Keep the fallback strict for real non-interactive cases: if stdin is redirected, piped, or the command is executing under a clearly headless context, call `cmd.Help()` instead of entering the wizard. Do not change the explicit `profile ...` subcommands or the existing `--env-file` behavior. Write table-driven tests in `internal/cli/root_test.go` by injecting stdin and environment probes rather than trying to fake a real TTY in-process.</action>
  <verify>
    <automated>go test ./internal/cli/...</automated>
  </verify>
  <done>No-arg startup reaches `StartInteractiveProfile` in expected Windows terminal contexts, still falls back to help for redirected stdin, and the behavior is locked by automated tests.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/cli/...` and confirm the root command starts the wizard in the simulated Windows-terminal cases while preserving help output for redirected stdin.</verification>

<success_criteria>
Phase 1's wizard-first entrypoint works in the Windows terminal environments used for UAT, and the startup branch selection is testable and regression-resistant.</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-04-SUMMARY.md`.
</output>