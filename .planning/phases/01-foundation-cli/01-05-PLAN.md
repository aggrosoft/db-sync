---
phase: 01-foundation-cli
plan: 05
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - internal/model/profile.go
  - internal/profile/schema.go
  - internal/secrets/template.go
  - internal/wizard/draft.go
  - internal/wizard/flow.go
  - internal/wizard/messages.go
  - internal/wizard/review.go
  - internal/wizard/wizard_test.go
autonomous: true
requirements:
  - PROF-01
  - PROF-03
gap_closure: true
must_haves:
  truths:
    - The create or edit flow lets the operator choose either a pasted connection string or guided connection details instead of authoring DSN templates by hand.
    - Edit mode reopens the same connection-first flow with the stored mode and non-secret values prefilled.
    - The review step explains the generated env-backed secret plan without printing raw passwords or raw DSNs.
  artifacts:
    - path: internal/model/profile.go
      provides: Operator-facing endpoint connection contract for connection-string and details modes.
    - path: internal/profile/schema.go
      provides: YAML shape for the connection-first endpoint representation.
    - path: internal/wizard/flow.go
      provides: Connection-first create and edit wizard flow.
    - path: internal/wizard/review.go
      provides: Secret-safe review summary for the new endpoint inputs.
    - path: internal/wizard/wizard_test.go
      provides: Automated coverage for the new operator flow and edit prefill behavior.
  key_links:
    - from: internal/wizard/draft.go
      to: internal/model/profile.go
      via: draft-to-profile conversion for connection-first endpoint input
      pattern: ToProfile|FromProfile
    - from: internal/wizard/flow.go
      to: internal/wizard/review.go
      via: create and edit flow feeds the review-before-save step
      pattern: review|confirm
    - from: internal/wizard/review.go
      to: internal/secrets/template.go
      via: generated env names and placeholder-safe summaries remain redacted
      pattern: Redact|Placeholder|ENV
---

<objective>
Close the DSN-template authoring gap at the operator interface by replacing Phase 1's template-only prompts with a connection-first endpoint model and wizard flow.

Purpose: The current schema and wizard force operators to think in placeholder templates instead of simply providing the connection information they already have.
Output: A connection-first endpoint contract plus create/edit/review wizard flow that captures operator input cleanly and sets up the env-backed save and validation work in the follow-on plan.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cli/01-CONTEXT.md
@.planning/phases/01-foundation-cli/01-UAT.md
@.planning/phases/01-foundation-cli/01-02-SUMMARY.md
@.planning/phases/01-foundation-cli/01-03-SUMMARY.md
@internal/model/profile.go
@internal/profile/schema.go
@internal/secrets/template.go
@internal/wizard/draft.go
@internal/wizard/flow.go
@internal/wizard/review.go

<interfaces>
From internal/model/profile.go:
```go
type Endpoint struct {
    Engine Engine `yaml:"engine"`
}

type Profile struct {
    Version   int
    Name      string
    Source    Endpoint
    Target    Endpoint
    Selection Selection
    Sync      SyncOptions
}
```

From internal/wizard/draft.go:
```go
type ProfileDraft struct {
    Name         string
    SourceEngine model.Engine
    TargetEngine model.Engine
    SyncMode     string
    MirrorDelete bool
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the connection-first endpoint contract and draft mapping</name>
  <files>internal/model/profile.go, internal/profile/schema.go, internal/secrets/template.go, internal/wizard/draft.go</files>
  <behavior>
    - Test 1: a newly created profile can represent either `connection-string` mode or `details` mode for each endpoint without the operator entering raw DSN templates.
    - Test 2: draft-to-profile and profile-to-draft conversion preserve the selected mode, non-secret values, and deterministic env-var naming for later save/load work.
    - Test 3: generated env references and any synthesized placeholders remain redacted in string output and never require raw-secret persistence in wizard state.
  </behavior>
  <action>Extend `internal/model/profile.go` so each endpoint can store operator-facing connection input in a first-class structure, not only `dsn_template`. Use a concrete contract that downstream work can implement directly: add `connection.mode` with allowed values `connection-string`, `details`, and `legacy-template`; for `connection-string`, keep the env-var name that will hold the full DSN; for `details`, keep host, port, database, username, password env-var name, and only the minimum engine-specific options needed to synthesize a valid PostgreSQL or MySQL/MariaDB DSN later. Update `internal/profile/schema.go` so the YAML shape reflects that richer endpoint contract without forcing save/load logic into this plan. Update `internal/wizard/draft.go` so drafts convert cleanly to and from the richer endpoint shape, and update `internal/secrets/template.go` so generated env references and synthesized placeholders remain redacted in summaries. Do not reintroduce raw DSN-template authoring as the primary operator model.</action>
  <verify>
    <automated>go test ./internal/profile/... ./internal/secrets/... ./internal/wizard/...</automated>
  </verify>
  <done>The domain model and draft layer can express simple connection input, and the secret-handling contract remains placeholder-safe for the new endpoint modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace DSN-template prompts with a fast-path connection flow for create and edit</name>
  <files>internal/wizard/flow.go, internal/wizard/messages.go, internal/wizard/review.go, internal/wizard/wizard_test.go</files>
  <behavior>
    - Test 1: the new flow lets each endpoint choose `Paste connection string` or `Enter connection details` and builds a valid draft for either path.
    - Test 2: edit mode reopens the same flow with the stored mode and values already populated.
    - Test 3: the review step shows host, port, database, username, env-var names, and env-file location, but never prints raw passwords or raw DSNs.
  </behavior>
  <action>Replace the `Source DSN template` and `Target DSN template` prompts in `internal/wizard/flow.go` with a connection-first sequence that keeps D-06's fast path: profile name, source engine, source input mode, source connection data, target engine, target input mode, target connection data, sync mode, mirror-delete, review, confirm. Per D-07, keep create and edit on the same flow with prefilled values rather than separate implementations. For `connection-string` mode, accept a pasted DSN once and derive a deterministic env-var name such as `DB_SYNC_<PROFILE_SLUG>_<ROLE>_DSN`. For `details` mode, ask for host, port, database, username, password, and only the minimum engine-specific options needed to synthesize a valid DSN for PostgreSQL or MySQL/MariaDB. Update `internal/wizard/review.go` plus `internal/wizard/messages.go` so operators see friendly connection summaries and the generated env-backed secret storage plan instead of placeholder-authoring instructions. Do not add save/load logic here; this plan ends at a correct review-ready draft.</action>
  <verify>
    <automated>go test ./internal/wizard/...</automated>
  </verify>
  <done>Operators can create or edit profiles through a connection-first fast path, and the review step explains the generated env-backed config without exposing secrets.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/wizard/... ./internal/profile/... ./internal/secrets/...` to prove the new connection modes are captured through the wizard and reviewed safely without bringing persistence or validation regressions into this slice.</verification>

<success_criteria>
Phase 1 no longer asks operators to author DSN templates manually during create or edit; instead it accepts operator-friendly connection data and produces a review-safe draft that the follow-on save/validation plan can persist.</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cli/01-05-SUMMARY.md`.
</output>
