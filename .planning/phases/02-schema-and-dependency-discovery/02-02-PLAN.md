---
phase: 02-schema-and-dependency-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/schema/drift.go
  - internal/schema/report.go
  - internal/schema/compare_test.go
autonomous: true
requirements:
  - SCMA-02
  - SCMA-03
  - SCMA-04
  - SCMA-05
must_haves:
  truths:
    - The tool can compare source and target snapshots per table and identify missing, extra, or incompatible columns without relying on raw DDL diffs.
    - Each table receives a clear `writable`, `writable-with-warning`, or `blocked` classification backed by column-level reasons.
    - Source columns with no usable target mapping are called out explicitly as skipped warnings.
    - Target-only required columns are treated as safe only when nullability or proven defaults make the table writable.
  artifacts:
    - path: internal/schema/drift.go
      provides: Snapshot comparison, reason codes, and table-level classification logic.
    - path: internal/schema/report.go
      provides: Drift report model consumable by later scan and sync phases.
    - path: internal/schema/compare_test.go
      provides: Automated classification coverage for missing, extra, incompatible, skipped, nullable, and default-backed scenarios.
  key_links:
    - from: internal/schema/drift.go
      to: internal/schema/snapshot.go
      via: normalized table and column facts
      pattern: Snapshot|TableID|Column
    - from: internal/schema/report.go
      to: internal/schema/drift.go
      via: drift results emitted in a downstream-consumable structure
      pattern: DriftReport|TableDrift|Classification
---

<objective>
Turn the snapshots from 02-01 into a deterministic drift analysis pipeline that detects structural mismatches and classifies table compatibility for future scan and sync work.

Purpose: Phase 2 only becomes operationally useful once the tool can explain what is writable, what is risky, and what is blocked before any row-level work starts.
Output: A reusable drift comparison engine, explicit reason codes, and table-level compatibility reports with automated coverage for Phase 2 schema requirements.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md
@.planning/phases/02-schema-and-dependency-discovery/02-RESEARCH.md
@.planning/phases/02-schema-and-dependency-discovery/02-VALIDATION.md
@.planning/phases/02-schema-and-dependency-discovery/02-01-PLAN.md
@internal/schema/snapshot.go
@internal/schema/service.go

<interfaces>
From internal/schema/snapshot.go:
```go
type Snapshot struct {
    Tables []Table
}

type Column struct {
    Name             string
    Ordinal          int
    Nullable         bool
    HasProvenDefault bool
    Identity         bool
    Generated        bool
    NativeType       string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compare normalized source and target snapshots with explicit drift reasons</name>
  <files>internal/schema/drift.go, internal/schema/compare_test.go</files>
  <read_first>.planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md, .planning/phases/02-schema-and-dependency-discovery/02-RESEARCH.md, internal/schema/snapshot.go, internal/schema/compare_test.go</read_first>
  <behavior>
    - Test 1: a source column missing from the target is captured as a skipped-source warning, not as an implicit schema change.
    - Test 2: a target column missing from the source is marked as blocking only when it is non-nullable and lacks a proven default, identity, or generated value.
    - Test 3: incompatible type or key-shape mismatches are reported against the canonical qualified table ID and column name.
  </behavior>
  <action>Create `internal/schema/drift.go` with comparison primitives that work from canonical tables and columns, not raw SQL or DDL text. Emit table-level and column-level reason structures for missing target columns, extra target columns, incompatible types, incompatible nullability, and key mismatches; keep skipped source-column reasons explicit per D-05 and preserve enough detail for later operator-facing summaries. Fill in `internal/schema/compare_test.go` with table-driven fixtures that cover PostgreSQL and MySQL-like normalized metadata without requiring live containers in this plan.</action>
  <verify>
    <automated>go test ./internal/schema/... -run 'TestCompareSnapshots|TestSkippedColumnsProduceWarnings'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type DriftReason struct|type TableDrift struct|type ColumnDrift struct' internal/schema/drift.go`
    - `rg 'skipped|missing|extra|incompatible' internal/schema/drift.go`
    - `rg 'TestCompareSnapshots|TestSkippedColumnsProduceWarnings' internal/schema/compare_test.go`
  </acceptance_criteria>
  <done>The codebase can explain structural schema differences in a normalized, engine-agnostic format that does not depend on parsing DDL or inventing schema changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify table compatibility as writable, warning, or blocked with downstream-ready reports</name>
  <files>internal/schema/drift.go, internal/schema/report.go, internal/schema/compare_test.go</files>
  <read_first>.planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md, .planning/phases/02-schema-and-dependency-discovery/02-VALIDATION.md, internal/schema/drift.go, internal/schema/snapshot.go</read_first>
  <behavior>
    - Test 1: a table with only skipped source columns is classified `writable-with-warning` and includes the skipped-column names.
    - Test 2: a table with target-only nullable or proven-default columns is classified `writable` or `writable-with-warning`, not blocked.
    - Test 3: a table with target-only required columns lacking nullability or proven defaults is classified `blocked` with concrete reasons.
  </behavior>
  <action>Extend the drift logic to produce Phase 2 classifications exactly as `writable`, `writable-with-warning`, or `blocked` per D-04 and D-06. Create `internal/schema/report.go` with a durable runtime report type that packages source snapshot identity, target snapshot identity, table classifications, warnings, blockers, and column-level explanations in a form later scan and sync phases can consume directly. Keep the implementation conservative about default handling: only mark `HasProvenDefault` true when adapter metadata actually proves it, and treat everything else as warning or blocked instead of guessing.</action>
  <verify>
    <automated>go test ./internal/schema/... -run 'TestClassifyTableDrift|TestTargetOptionalColumns'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'writable-with-warning|blocked|writable' internal/schema/drift.go internal/schema/report.go`
    - `rg 'SkippedColumns|Warnings|Blockers|Classification' internal/schema/report.go`
    - `rg 'TestClassifyTableDrift|TestTargetOptionalColumns' internal/schema/compare_test.go`
  </acceptance_criteria>
  <done>Phase 2 can produce deterministic compatibility classifications that preserve operator-visible reasoning and can be reused by later planning, scan, and sync workflows.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/schema/... -run 'TestCompareSnapshots|TestClassifyTableDrift|TestSkippedColumnsProduceWarnings|TestTargetOptionalColumns'` and confirm the report model expresses all four schema-analysis requirements without requiring new adapters or CLI work.</verification>

<success_criteria>
Given two canonical snapshots from 02-01, the repository can detect drift, classify each table correctly, and explain skipped, warning, and blocked outcomes in a reusable report model.</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-and-dependency-discovery/02-02-SUMMARY.md`.
</output>