---
phase: 02-schema-and-dependency-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/schema/graph.go
  - internal/schema/selection.go
  - internal/schema/graph_test.go
  - internal/model/profile.go
  - internal/profile/schema.go
  - internal/wizard/draft.go
  - internal/wizard/flow.go
  - internal/wizard/review.go
  - internal/cli/table_selection_test.go
autonomous: false
requirements:
  - TABL-01
  - TABL-02
  - TABL-03
must_haves:
  truths:
    - The tool can build a directed foreign-key dependency graph from discovered schema metadata and compute the minimum required dependent-table set for explicit operator selections.
    - Operators can include or exclude tables interactively while seeing which dependencies are auto-required and which exclusions leave the plan blocked.
    - Saved profiles persist only the operator’s durable selection intent, not a stale precomputed dependency closure or schema snapshot.
    - The review output makes dependency additions and blocked exclusions visible before save.
  artifacts:
    - path: internal/schema/graph.go
      provides: Directed foreign-key graph and closure logic.
    - path: internal/schema/selection.go
      provides: Selection preview model that distinguishes explicit includes, required additions, and blocked exclusions.
    - path: internal/model/profile.go
      provides: Durable persisted selection-intent fields for Phase 2.
    - path: internal/wizard/flow.go
      provides: Dependency-aware selection step inside the existing create and edit wizard flow.
    - path: internal/cli/table_selection_test.go
      provides: Automated coverage for dependency-aware selection behavior and profile round-trip intent preservation.
  key_links:
    - from: internal/schema/selection.go
      to: internal/schema/graph.go
      via: dependency closure from explicit table choices
      pattern: RequiredTables|BlockedExclusions|Closure
    - from: internal/wizard/flow.go
      to: internal/schema/selection.go
      via: interactive selection and blocked-state review
      pattern: SelectionPreview|RequiredTables
    - from: internal/wizard/draft.go
      to: internal/model/profile.go
      via: persisted explicit selection intent round-trip
      pattern: Selection
---

<objective>
Implement the dependency-graph and interactive selection slice of Phase 2 so operators can choose tables with visible foreign-key implications and persist only their durable intent.

Purpose: Safe sync planning depends on table selection being dependency-aware; without this, operators are still guessing at required parent tables and blocked exclusions.
Output: Graph closure logic, a selection-preview model, persisted explicit selection intent in profiles, and wizard flow changes that show required additions and blocked exclusions before save.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md
@.planning/phases/02-schema-and-dependency-discovery/02-RESEARCH.md
@.planning/phases/02-schema-and-dependency-discovery/02-VALIDATION.md
@.planning/phases/02-schema-and-dependency-discovery/02-01-PLAN.md
@internal/schema/snapshot.go
@internal/model/profile.go
@internal/profile/schema.go
@internal/wizard/draft.go
@internal/wizard/flow.go
@internal/wizard/review.go

<interfaces>
From internal/model/profile.go:
```go
type Selection struct {
    Tables []string `yaml:"tables"`
}
```

From internal/wizard/review.go:
```go
fmt.Sprintf("Tables: %v", profile.Selection.Tables)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the foreign-key dependency graph and minimum-closure selection preview</name>
  <files>internal/schema/graph.go, internal/schema/selection.go, internal/schema/graph_test.go</files>
  <read_first>.planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md, .planning/phases/02-schema-and-dependency-discovery/02-RESEARCH.md, internal/schema/snapshot.go, internal/schema/graph_test.go</read_first>
  <behavior>
    - Test 1: foreign-key edges are directed from dependent child table to required parent table and preserve composite-key ordering.
    - Test 2: the minimum dependency closure for an explicit seed-table list includes only the required parents, not unrelated tables.
    - Test 3: excluding a required dependency keeps the exclusion visible and marks the resulting selection preview as blocked instead of silently fixing it.
  </behavior>
  <action>Create `internal/schema/graph.go` with a directed foreign-key graph built from the canonical snapshot produced in 02-01, and create `internal/schema/selection.go` with a `SelectionPreview`-style runtime model that separates explicit includes, required additions, blocked exclusions, and final valid order. Fill in `internal/schema/graph_test.go` to cover simple and composite foreign keys, minimum closure behavior, and the D-09 blocked-exclusion rule. Keep the graph limited to foreign-key relationships only; do not expand scope to triggers, procedures, views, or cross-engine mapping.</action>
  <verify>
    <automated>go test ./internal/schema/... -run 'TestBuildDependencyGraph|TestDependencyClosure'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type Graph struct|type SelectionPreview struct|BlockedExclusions|RequiredTables' internal/schema/graph.go internal/schema/selection.go`
    - `rg 'TestBuildDependencyGraph|TestDependencyClosure' internal/schema/graph_test.go`
    - `rg 'foreign key|closure|blocked exclusion|composite' internal/schema/graph_test.go`
  </acceptance_criteria>
  <done>The repository can compute dependency-aware table-selection previews that preserve operator intent and expose blocked exclusions without mutating saved state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist explicit table-selection intent and wire dependency-aware selection into the existing wizard flow</name>
  <files>internal/model/profile.go, internal/profile/schema.go, internal/wizard/draft.go, internal/wizard/flow.go, internal/wizard/review.go, internal/cli/table_selection_test.go</files>
  <read_first>.planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md, .planning/phases/02-schema-and-dependency-discovery/02-VALIDATION.md, internal/model/profile.go, internal/profile/schema.go, internal/wizard/draft.go, internal/wizard/flow.go, internal/wizard/review.go</read_first>
  <behavior>
    - Test 1: profile persistence round-trips explicit selected tables and any explicit exclusion intent without storing computed closures or schema snapshots.
    - Test 2: the create and edit wizard flows show required dependency additions and blocked exclusions before save.
    - Test 3: review output names explicit selections, required additions, and blocked exclusions instead of printing only a raw table slice.
  </behavior>
  <action>Extend the Phase 1 profile model and normalization logic so it persists only durable operator intent per D-10: explicit selected tables and any explicit dependency-exclusion intent needed to preserve blocked choices across edit cycles. Update `internal/wizard/draft.go`, `internal/wizard/flow.go`, and `internal/wizard/review.go` so the existing create/edit flow performs schema discovery after connection validation, lets the operator seed the table list, shows the minimum required dependency set, preserves visible blocked exclusions per D-08 and D-09, and renders a clear review summary instead of `Tables: %v`. Fill in `internal/cli/table_selection_test.go` with automated coverage for selection preview behavior and profile round-trip semantics; keep the interaction staged and terminal-friendly rather than turning it into a giant flat checklist. Before the human-verification checkpoint, have the task prepare a seeded verification fixture and launch the updated interactive flow so the operator is only judging clarity and blocked-dependency behavior, not doing manual environment setup.</action>
  <verify>
    <automated>go test ./internal/cli/... ./internal/wizard/... ./internal/profile/... -run 'TestDependencySelection|Test.*Selection'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'Selection struct|Selected|Excluded|tables' internal/model/profile.go`
    - `rg 'dependency|required|blocked|selection' internal/wizard/flow.go internal/wizard/review.go`
    - `rg 'TestDependencySelection' internal/cli/table_selection_test.go`
    - `rg 'Tables: %v' internal/wizard/review.go` returns no matches
  </acceptance_criteria>
  <done>Operators can create or edit profiles with dependency-aware table selection, and saved profiles preserve their intent without storing stale discovery results.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify dependency-aware table selection flow</name>
  <files>internal/wizard/draft.go, internal/wizard/flow.go, internal/wizard/review.go, internal/model/profile.go</files>
  <action>Pause after Task 2 once the agent has prepared the seeded verification fixture and launched the updated interactive profile flow. Ask the operator to evaluate only terminal readability, dependency explanations, and blocked-exclusion behavior using the running flow.</action>
  <verify>
    <manual>The operator responds `approved` only after confirming the launched interactive flow shows required additions, keeps blocked exclusions visible, and round-trips saved intent cleanly in edit mode.</manual>
  </verify>
  <done>The manual checkpoint has either been approved for terminal usability or has captured concrete feedback that the executor can use to continue iteration.</done>
  <what-built>Dependency-aware interactive table selection inside the existing profile wizard, including a seeded verification fixture, required-table suggestions, and visible blocked exclusions.</what-built>
  <how-to-verify>
    1. Use the already launched interactive profile flow against the seeded fixture schema containing multiple foreign-key chains and at least one composite foreign key.
    2. Select a small seed set of tables and confirm the review step shows the minimum required dependent tables added by the tool.
    3. Explicitly exclude one required parent table and confirm the UI keeps the exclusion visible while marking the selection blocked rather than silently correcting it.
    4. Save and reopen the profile in edit mode and confirm the explicit selections and blocked exclusion intent round-trip cleanly.
  </how-to-verify>
  <resume-signal>Type "approved" if the terminal flow is readable and the blocked-dependency behavior matches the plan, or describe the issue to continue from the failing step.</resume-signal>
</task>

</tasks>

<verification>
Run `go test ./internal/schema/... -run 'TestBuildDependencyGraph|TestDependencyClosure'` and `go test ./internal/cli/... ./internal/wizard/... ./internal/profile/... -run 'TestDependencySelection|Test.*Selection'`, then complete the human verification checkpoint for terminal usability.</verification>

<success_criteria>
Phase 2 can generate dependency-aware table choices, preserve explicit operator intent in the profile, and show blocked dependency exclusions clearly enough for operators to understand the sync plan before save.</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-and-dependency-discovery/02-03-SUMMARY.md`.
</output>
