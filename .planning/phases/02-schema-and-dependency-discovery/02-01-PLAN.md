---
phase: 02-schema-and-dependency-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/schema/snapshot.go
  - internal/schema/service.go
  - internal/schema/snapshot_test.go
  - internal/schema/compare_test.go
  - internal/schema/graph_test.go
  - internal/db/postgres/adapter.go
  - internal/db/postgres/adapter_integration_test.go
  - internal/db/mysql/adapter.go
  - internal/db/mysql/adapter_integration_test.go
  - internal/db/mysql/mariadb_integration_test.go
  - internal/validate/endpoint.go
  - internal/validate/service.go
  - internal/cli/table_selection_test.go
autonomous: true
requirements:
  - SCMA-01
must_haves:
  truths:
    - The tool can read table, column, default, key, and foreign-key metadata from supported PostgreSQL and MySQL/MariaDB endpoints without mutating either database.
    - Discovery reuses the same endpoint resolution semantics as validation, so source and target introspection run against the real saved profile connection inputs.
    - Missing metadata privileges or incomplete catalog visibility surface as blocked discovery results with remediation guidance instead of silent partial success.
    - Wave 0 test files exist before the rest of Phase 2 implementation so later tasks have executable verification targets.
  artifacts:
    - path: internal/schema/snapshot.go
      provides: Canonical schema snapshot model with qualified table identity and metadata fields needed by later drift and dependency logic.
    - path: internal/schema/service.go
      provides: Discovery orchestration that resolves profile endpoints and requests schema snapshots through engine adapters.
    - path: internal/validate/endpoint.go
      provides: Shared endpoint resolution helper reused by validation and discovery.
    - path: internal/db/postgres/adapter.go
      provides: PostgreSQL read-only schema discovery methods layered onto the existing adapter.
    - path: internal/db/mysql/adapter.go
      provides: MySQL/MariaDB read-only schema discovery methods layered onto the existing adapter.
    - path: internal/schema/snapshot_test.go
      provides: Wave 0 normalization and qualified-table-ID test coverage.
  key_links:
    - from: internal/schema/service.go
      to: internal/validate/endpoint.go
      via: shared endpoint resolution for source and target discovery
      pattern: ResolveEndpoint|resolveEndpoint
    - from: internal/schema/service.go
      to: internal/db/postgres/adapter.go
      via: adapter-backed snapshot collection
      pattern: DiscoverSchema
    - from: internal/schema/service.go
      to: internal/db/mysql/adapter.go
      via: adapter-backed snapshot collection
      pattern: DiscoverSchema
---

<objective>
Create the shared discovery foundation for Phase 2: Wave 0 test scaffolding, canonical schema snapshot contracts, and adapter-backed read-only introspection for supported engines.

Purpose: Every later drift and dependency feature depends on a correct, reusable schema model and on discovery using the same connection semantics already proven in Phase 1.
Output: Named Wave 0 test files, a shared schema snapshot package, a discovery service that reuses endpoint resolution, and PostgreSQL/MySQL/MariaDB adapter discovery methods.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md
@.planning/phases/02-schema-and-dependency-discovery/02-RESEARCH.md
@.planning/phases/02-schema-and-dependency-discovery/02-VALIDATION.md
@.planning/phases/01-foundation-cli/01-01-PLAN.md
@.planning/phases/01-foundation-cli/01-06-PLAN.md
@internal/db/postgres/adapter.go
@internal/db/mysql/adapter.go
@internal/validate/service.go
@internal/model/profile.go

<interfaces>
From internal/validate/service.go:
```go
type Adapter interface {
    ValidateSource(ctx context.Context, resolvedDSN string, engine model.Engine) (profile.EndpointValidation, error)
    ValidateTarget(ctx context.Context, resolvedDSN string, engine model.Engine) (profile.EndpointValidation, error)
}
```

From internal/model/profile.go:
```go
type Selection struct {
    Tables []string `yaml:"tables"`
}

type Profile struct {
    Name      string
    Source    Endpoint
    Target    Endpoint
    Selection Selection
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 schema discovery test scaffolding and fixtures</name>
  <files>internal/schema/snapshot_test.go, internal/schema/compare_test.go, internal/schema/graph_test.go, internal/db/postgres/adapter_integration_test.go, internal/db/mysql/adapter_integration_test.go, internal/db/mysql/mariadb_integration_test.go, internal/cli/table_selection_test.go</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md, .planning/phases/02-schema-and-dependency-discovery/02-RESEARCH.md, .planning/phases/02-schema-and-dependency-discovery/02-VALIDATION.md, internal/testkit/containers.go, internal/db/postgres/adapter.go, internal/db/mysql/adapter.go</read_first>
  <action>Create the Wave 0 test files called out in `02-VALIDATION.md` before production discovery code expands. Add named test entry points matching the validation map: `TestNormalizeSnapshot`, `TestCompareSnapshots`, `TestClassifyTableDrift`, `TestSkippedColumnsProduceWarnings`, `TestTargetOptionalColumns`, `TestBuildDependencyGraph`, `TestDependencyClosure`, and `TestDependencySelection`; add integration test skeletons for PostgreSQL, MySQL, and MariaDB discovery using the existing `internal/testkit` container helpers and build tags consistent with the current integration suite. Make the scaffolding compile cleanly and use table fixtures or `t.Skip` placeholders where later tasks need to fill assertions, but do not invent production behavior in the tests yet.</action>
  <verify>
    <automated>go test ./internal/schema/... ./internal/cli/...; go test -tags=integration ./internal/db/... -run 'Test(Postgres|MySQL|MariaDB)'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'TestNormalizeSnapshot|TestCompareSnapshots|TestClassifyTableDrift|TestSkippedColumnsProduceWarnings|TestTargetOptionalColumns' internal/schema`
    - `rg 'TestBuildDependencyGraph|TestDependencyClosure' internal/schema/graph_test.go`
    - `rg 'TestDependencySelection' internal/cli/table_selection_test.go`
    - `rg 'Test.*Postgres|Test.*MySQL|Test.*MariaDB' internal/db`
  </acceptance_criteria>
  <done>All Wave 0 test files required by the phase validation strategy exist, compile, and establish the exact named verification targets the remaining Phase 2 plans will fill in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define canonical schema snapshot contracts and shared discovery orchestration</name>
  <files>internal/schema/snapshot.go, internal/schema/service.go, internal/schema/snapshot_test.go, internal/validate/endpoint.go, internal/validate/service.go</files>
  <read_first>.planning/phases/02-schema-and-dependency-discovery/02-CONTEXT.md, .planning/phases/02-schema-and-dependency-discovery/02-RESEARCH.md, internal/validate/service.go, internal/model/profile.go, internal/profile/schema.go</read_first>
  <behavior>
    - Test 1: normalization preserves qualified table identity such as `public.users` or `app.orders`, not bare table names only.
    - Test 2: snapshot columns expose nullable, default, identity, generated, and native-type facts needed by SCMA-02 through SCMA-05.
    - Test 3: discovery orchestration returns blocked results with remediation guidance when endpoint resolution or metadata access fails.
  </behavior>
  <action>Create `internal/schema/snapshot.go` with canonical `TableID`, `Snapshot`, `Table`, `Column`, `PrimaryKey`, and `ForeignKey` structs that preserve schema-qualified table identity, column order, and writeability facts called for by D-02, D-04, D-06, and D-12. Create `internal/schema/service.go` with a discovery service that accepts a normalized Phase 1 profile, resolves source and target endpoints through a helper extracted into `internal/validate/endpoint.go`, and requests schema snapshots through adapter-backed methods rather than duplicating connection logic. Update `internal/validate/service.go` to consume the shared resolver so validation and discovery cannot drift on env-backed secret handling or connection-mode semantics.</action>
  <verify>
    <automated>go test ./internal/schema/... ./internal/validate/... -run 'TestNormalizeSnapshot|Test.*Endpoint'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type TableID struct|type Snapshot struct|type Column struct|type ForeignKey struct' internal/schema/snapshot.go`
    - `rg 'Nullable|HasProvenDefault|Identity|Generated|NativeType' internal/schema/snapshot.go`
    - `rg 'type Service struct|DiscoverProfile|DiscoverEndpoints' internal/schema/service.go`
    - `rg 'ResolveEndpoint|resolveEndpoint' internal/validate`
  </acceptance_criteria>
  <done>The repository has a shared discovery model and orchestration path that later plans can build on without re-solving table identity or endpoint resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the existing PostgreSQL and MySQL adapters with read-only schema introspection</name>
  <files>internal/db/postgres/adapter.go, internal/db/postgres/adapter_integration_test.go, internal/db/mysql/adapter.go, internal/db/mysql/adapter_integration_test.go, internal/db/mysql/mariadb_integration_test.go, internal/schema/service.go</files>
  <read_first>.planning/phases/02-schema-and-dependency-discovery/02-RESEARCH.md, internal/db/postgres/adapter.go, internal/db/mysql/adapter.go, internal/testkit/containers.go, internal/schema/snapshot.go, internal/schema/service.go</read_first>
  <behavior>
    - Test 1: PostgreSQL discovery returns non-system schemas, qualified table IDs, and ordered column metadata from read-only catalog queries.
    - Test 2: MySQL and MariaDB discovery return ordered column metadata and foreign-key metadata from the shared information-schema baseline plus only the narrow supplements needed for correctness.
    - Test 3: metadata permission failures produce blocked discovery output with remediation detail instead of partial success.
  </behavior>
  <action>Add discovery methods to the existing adapters per D-01 through D-03, using an information-schema-first baseline and only the engine-specific supplements described in research for PostgreSQL type fidelity and FK metadata or MySQL/MariaDB default and FK-order fidelity. Keep the implementation non-mutating, route MariaDB through the shared MySQL adapter path unless a verified compatibility gap requires a branch, and return typed blocked-discovery errors when metadata visibility is insufficient. Update the new integration tests to seed representative schemas through `internal/testkit` and verify column ordering, qualified table IDs, foreign-key extraction, and metadata-permission failure handling.</action>
  <verify>
    <automated>go test ./internal/schema/...; go test -tags=integration ./internal/db/... ./internal/validate/... -run 'Test(Postgres|MySQL|MariaDB)'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'DiscoverSchema|DiscoverSourceSchema|DiscoverTargetSchema' internal/db/postgres/adapter.go internal/db/mysql/adapter.go`
    - `rg 'information_schema.columns|pg_constraint|key_column_usage|referential_constraints' internal/db/postgres/adapter.go internal/db/mysql/adapter.go`
    - `rg 'metadata|permission|blocked|remediation' internal/schema/service.go internal/db`
    - `go test -tags=integration ./internal/db/...`
  </acceptance_criteria>
  <done>Supported adapters can produce canonical read-only schema snapshots for source and target endpoints, and the validation strategy has real integration coverage for the discovery foundation.</done>
</task>

</tasks>

<verification>
Run `go test ./internal/schema/... ./internal/validate/... ./internal/cli/...` and `go test -tags=integration ./internal/db/...` to prove the Wave 0 scaffolding, shared discovery contracts, and adapter-backed introspection all compile and execute through the repository’s real validation/discovery seams.</verification>

<success_criteria>
Phase 2 has a reusable discovery substrate: tests exist, canonical snapshots exist, and PostgreSQL plus MySQL/MariaDB adapters can read metadata safely enough for downstream drift and dependency plans.</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-and-dependency-discovery/02-01-SUMMARY.md`.
</output>